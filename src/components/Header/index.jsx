import React from 'react';

import './Header.scss';

//Todo: Create a normal header
const Header = () => (
    <header>
        <div className='container'>
            Header
        </div>
    </header>
);

export default Header;